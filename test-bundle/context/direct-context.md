# Direct Context

This context file is included directly via `context.include` in bundle.md's frontmatter.

## Verification Token

**BUNDLE_DIRECT_CONTEXT_ALPHA**

If you can see this token, direct @mention resolution is working correctly.

This validates that the `@namespace:path` syntax in markdown body correctly resolves and includes the referenced content.
