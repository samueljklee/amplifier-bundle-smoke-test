# Behavior-Contributed Context

This context file is contributed by `contributing-behavior.yaml` via `context.include`.

## Verification Token

**BEHAVIOR_TRANSITIVE_CONTEXT_BETA**

If you can see this token, transitive context resolution from included behaviors is working correctly.

This validates that behaviors can contribute context to their parent bundle via the `context.include` mechanism.
